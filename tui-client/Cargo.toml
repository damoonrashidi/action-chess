[package]
name = "client"
version = "0.1.0"
authors.workspace = true
edition.workspace = true

[dependencies]
state = { path = "../state" }
network = { path = "../network" }

[[bin]]
name = "client"
path = "main.rs"
